[package]
name = "{{ cookiecutter.project_slug }}"
version = "0.1.0"
edition = "2021"

[lib]
name = "{{ cookiecutter.project_slug }}"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.18.0", features = ["extension-module"] }
